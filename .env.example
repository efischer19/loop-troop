# Fine-grained Personal Access Token for a dedicated GitHub Machine Account.
# Keep this in the native control plane environment only; never pass it to containers.
GITHUB_PAT=github_pat_your_machine_account_token

# Poll interval in seconds for GitHub API checks.
LOOP_TROOP_POLL_INTERVAL=60
